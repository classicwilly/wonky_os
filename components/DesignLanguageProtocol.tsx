import React from 'react';
import ProtocolView from './ProtocolView.js';

const DesignLanguageProtocol = () => {
  return (
    <ProtocolView
      sourceDocument="Design Language Protocol"
      title="SOP-DESIGN-001: OS Design Language"
      subtitle="This protocol codifies the visual and interaction design rules—colors, typography, component philosophy—to ensure a consistent and cohesive user experience."
    />
  );
};

export default DesignLanguageProtocol;